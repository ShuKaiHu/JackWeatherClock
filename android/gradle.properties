# JVM arguments for the Gradle daemon.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# AndroidX is required by AGP 8.
android.useAndroidX=true
kotlin.code.style=official

# AdMob application ID. The default is Google's public sample ID so debug
# builds initialize without a real account. Override with the production
# Android app ID from the AdMob console before any Play upload:
#   ./gradlew bundleRelease -PadmobAppId=ca-app-pub-XXXX~YYYY -PadmobBannerAdUnitId=ca-app-pub-XXXX/ZZZZ
admobAppId=ca-app-pub-3940256099942544~3347511713
admobBannerAdUnitId=
