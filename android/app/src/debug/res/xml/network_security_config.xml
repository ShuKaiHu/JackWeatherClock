<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug only. The weather proxy runs on the developer's machine during
  development, reached from the emulator as 10.0.2.2 over plain HTTP. Release
  builds never see this file, so cleartext stays blocked everywhere that ships.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
